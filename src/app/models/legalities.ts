export class Legalities {
    format: string;
    legality: string;

    constructor(format: string, legality: string) {
        this.format = format;
        this.legality = legality;
    }
}
